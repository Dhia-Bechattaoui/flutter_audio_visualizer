/// Constants used for visualization appearance and behavior.
class VisualizationConstants {
  /// Default bar width for visualization bars
  static const double defaultBarWidth = 3;

  /// Default spacing between visualization bars
  static const double defaultBarSpacing = 1;

  /// Default animation duration for transitions
  static const Duration defaultAnimationDuration = Duration(milliseconds: 200);

  /// Default number of bars for visualization
  static const int defaultBarCount = 64;

  /// Default height for visualization bars
  static const double defaultBarHeight = 100;

  /// Default corner radius for visualization bars
  static const double defaultCornerRadius = 2;

  /// Default shadow blur radius
  static const double defaultShadowBlurRadius = 4;

  /// Default shadow offset
  static const double defaultShadowOffset = 2;

  /// Default shadow opacity
  static const double defaultShadowOpacity = 0.3;

  /// Default gradient start color (blue)
  static const int defaultGradientStartColor = 0xFF2196F3;

  /// Default gradient end color (purple)
  static const int defaultGradientEndColor = 0xFF9C27B0;
}
